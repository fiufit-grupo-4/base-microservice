codecov:
    bot: codecovbot

coverage:
    status:
        project:
            default: off
        patch:
            default: off

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false  
  require_base: no
  require_head: no       
  branches: null
